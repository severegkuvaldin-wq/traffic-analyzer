# 🛡️ Анализатор сетевого трафика для выявления подозрительной активности

Проект разработан **Кувалдиным Иваном Евгеньевичем**, учащимся 11 класса МБОУ СОШ №3 г. Ноябрьска,  
для участия в муниципальном этапе **Всероссийской олимпиады школьников по информатике и информационной безопасности**.

Система обнаруживает аномалии в реальном времени:
- Подозрительные DNS-запросы (длинные, случайные домены)
- Соединения с известными вредоносными IP (Feodo Tracker)
- Использование опасных портов (4444, 1337, 6667, 5555, 8443)
- Анализ SNI в TLS-трафике (HTTPS)

Поддерживает **Windows и Linux**.

---

## 📁 Содержимое репозитория

| Файл                     | Описание                                       |
|--------------------------|------------------------------------------------|
| `analyzer_with_blocker.py`   | Версия с интерактивной блокировкой угроз (требует прав администратора) |
| `analyzer_without_blocker.py`| Версия без блокировки — только мониторинг и логирование |
| `requirements.txt`       | Зависимости проекта                            |
| `.gitignore`             | Исключённые файлы (логи, временные файлы)      |
| `LICENSE`                | Лицензия проекта (GNU GPL v3.0)                |

---

## ⚙️ Установка

1. Установите **Python 3.7 или новее**.
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

### Для Windows:
- **Обязательно установите Npcap** — драйвер для перехвата сетевого трафика:  
  🔗 [https://npcap.com](https://npcap.com)  
  → При установке выберите опцию:  
  **"Install Npcap in WinPcap API-compatible Mode"**.
- Запускайте скрипты **от имени Администратора** (через PowerShell).

### Для Linux:
- Установите `libpcap-dev`:
  ```bash
  # Ubuntu / Debian
  sudo apt update && sudo apt install libpcap-dev
  ```
- Запускайте через `sudo`.

---

## ▶️ Запуск

### 🔒 С блокировкой (рекомендуется для демонстрации):
```bash
# Windows (PowerShell от Администратора)
python analyzer_with_blocker.py

# Linux
sudo python3 analyzer_with_blocker.py
```

При обнаружении угрозы появится меню:
```
[1] Да, заблокировать
[2] Нет, пропустить
[3] Заблокировать и не спрашивать больше
```

### 👀 Без блокировки (безопасно для тестирования):
```bash
# Windows
python analyzer_without_blocker.py

# Linux
python3 analyzer_without_blocker.py
```

> Не требует прав администратора. Все события записываются в файл `alerts.log`.

---

## 🧪 Как протестировать

1. **Опасный порт**:  
   ```bash
   telnet 127.0.0.1 4444
   ```
   → вызовет предупреждение: *«Metasploit reverse shell»*.

2. **Аномальный DNS-флуд**:  
   Выполните в другом окне терминала:
   ```python
   from scapy.all import *
   for i in range(35):
       send(IP(dst="8.8.8.8")/UDP(dport=53)/DNS(rd=1,qd=DNSQR(qname=f"{'x'*40}{i}.test")), verbose=0)
   ```
   → вызовет алерт: *«Аномальная DNS-активность»*.

3. **Завершение работы**:  
   Нажмите **Ctrl+C** — все правила брандмауэра будут автоматически удалены (Windows).

---

## 📜 Лицензия

Проект распространяется под лицензией **GNU General Public License v3.0**.  
Разрешено использовать в образовательных целях, на олимпиадах и в школьных проектах.

© Кувалдин Иван Евгеньевич, МБОУ СОШ №3 г. Ноябрьска, 2026
